# Service runtime dependencies for the exoplanet-triage API.
# Pinned to exact versions for reproducible Docker builds.

# Web framework + server
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4

# Model loading + inference
# (xgboost, scikit-learn, pyarrow are imported indirectly when joblib
#  unpickles the bundle: the XGBClassifier, LabelEncoder, and demo_sample
#  DataFrame each pull in one of these.)
joblib==1.5.3
scikit-learn==1.9.0
xgboost==3.4.0
pandas==3.0.5
numpy==2.4.6
pyarrow==25.0.0